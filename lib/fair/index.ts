/**
 * lib/fair — barrel export for the deterministic round engine.
 *
 * Re-exports every pure function and constant from engine.ts. Import from
 * `@/lib/fair` (or relative `../fair`) rather than reaching into engine.ts.
 */

export * from './engine';
